# Missing Static Files

The following files are referenced in the templates but are missing:

- favicon.ico: A favicon icon file (typically 16x16 or 32x32 pixels) for the browser tab.
- africana-ai-preview.png: A preview image for social media sharing (Open Graph image), recommended size 1200x630 pixels.

Please add these files to this directory (theme/static/images/).

After adding the files, rebuild the Docker image and redeploy to include them in the static files.